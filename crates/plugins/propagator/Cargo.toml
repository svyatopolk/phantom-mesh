[package]
name = "propagator"
version = "0.1.0"
edition = "2021"

[dependencies]
loader = { path = "../../loader" }
tokio = { version = "1", features = ["full"] }
log = "0.4"
env_logger = "0.10"
local-ip-address = "0.6.8"
